<% if @users.is_normal_user? %> 

<div class="col-xs-12">
	<div class="col-xs-8">
<p>
  <strong>Usuario:</strong>
  <%= @users.email %>
</p>
<p>
	</div>
	<div class="col-xs-4"><p class="text-center">
  <strong>Lista de Seguidos:</strong>
    <% @users.followers.each do |follower| %>
	<li><%= follower.seguido.email %>
	<% if current_user.email == @users.email %> 
		<%= link_to '(Dejar de Seguir)', follower, :method => :delete %></li> 
    <% end %>
    <% end %>

    <p class="text-center"><strong>Lista de Seguidores:</strong>	</p>


    <% @users.inverse_seguidos.each do |user| %>
	<li><%= user.email %>

    <% end %>

	</p>
	</div>
</div>

</p>

<% end %>
